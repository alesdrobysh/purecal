# PureCal Release Script Configuration
# Copy this file to .release.config and customize as needed
# Note: .release.config is gitignored to protect API keys

# LLM Backend Configuration
# Options: auto (default), gemini, ollama
# - auto: Prefer Gemini API if GEMINI_API_KEY is set, otherwise use Ollama
# - gemini: Use Google Gemini API (requires API key)
# - ollama: Use local Ollama (requires ollama installed)
LLM_BACKEND=auto

# Gemini API Settings (Primary LLM)
# Get a free API key at: https://ai.google.dev/
GEMINI_API_KEY=your-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp

# Ollama Settings (Fallback LLM)
# Install from: https://ollama.ai/
OLLAMA_MODEL=llama3:latest

# Build and Release Options
BUILD_APK=yes              # Build Android APK after versioning (yes/no)
CREATE_GITHUB_RELEASE=yes  # Create GitHub release with APK (yes/no)
PUSH_TO_REMOTE=yes         # Push commit and tag to remote (yes/no)
